# kiho runtime learnings — {{project_name}}

<!--
This file is the ralph-style @AGENT.md for kiho's CEO. It holds durable
runtime knowledge that survives across /kiho invocations: build commands,
known quirks, last-session summary, cross-session reminders.

CEO reads this at the start of every /kiho turn and appends as it learns
things during delegation. Never delete entries without an explicit reason;
move stale entries to "Archived" at the bottom.
-->

## Build commands
(to be discovered by CEO on first use; typically includes):
- test:
- lint:
- typecheck:
- build:
- run:

## Known quirks
(CEO adds entries as it encounters repo-specific behaviors the LLM wouldn't guess)

## Last session summary
(CEO writes a one-paragraph summary on session close)

## Cross-session reminders
(things CEO wants its future self to remember; format: `- [date] <note>`)

## Archived
(stale entries moved here manually or by memory-consolidate)
