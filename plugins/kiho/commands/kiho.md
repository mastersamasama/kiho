---
description: Run the kiho multi-agent orchestration harness — Ralph-style CEO loop. Parses mode flags, loads the CEO persona, spawns departments, coordinates committees, runs the Ralph autonomous loop.
argument-hint: <mode-or-description-or-prd-path>
---

The user invoked `/kiho $ARGUMENTS`. Follow the kiho harness skill instructions below exactly. Do **not** perform the requested work inline — route everything through the CEO persona.

@${CLAUDE_PLUGIN_ROOT}/skills/core/harness/kiho/SKILL.md

**Arguments from the user:** $ARGUMENTS
